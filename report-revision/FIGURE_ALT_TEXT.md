# Stage 3 Figure Captions and Alt Text

1. **Vision Auto evolution timeline.** Caption: "Evolution from single-image OCR to dish-first discovery, showing the problem, engineering change, outcome, and remaining limitation at each stage." Alt text: "Five-stage horizontal timeline: image OCR, video sampling, Track 1 and Track 2 routing, candidate-first review, and dish-first discovery. Each card lists its problem, change, outcome, and limitation."
2. **`useVisionAuto` state machine.** Caption: "Reactive lifecycle and terminal uncertainty states managed by the Vue composable; backend stages are adapted before reaching the UI." Alt text: "State diagram from Idle to Queued, Fast analysis, Deep analysis, and Resolving, branching to Completed, Review, Not found, Timeout, or Cancelled, with retry and reset returning to a new run or Idle."
3. **Stage 3 sequence diagram.** Caption: "Coordination between `FoodMapView`, the composable, asynchronous job API, backend coordinator/worker, response adapter, and reactive Leaflet UI." Alt text: "Sequence diagram showing create job, coordinator-worker progress, frontend polling, response adaptation, reactive map update, and alternate paths for AbortController cancellation, backend DELETE cancellation, stale response rejection, and unmount cleanup."
4. **Track 2 V3 versus Final router benchmark.** Caption: "Same 30 labelled router cases replayed five measured times per version; percentage and latency use separate panels and this is not full Vision Auto latency or accuracy." Alt text: "Two-panel chart. Top panel shows both versions at 96.67 percent safe routing and zero percent false promotion. Bottom panel shows router p50 near 0.05 milliseconds and p95 near 0.19 milliseconds for both versions."
5. **Test-to-concern matrix.** Caption: "Deterministic evidence linking lifecycle and safety concerns to the focused frontend, backend coordinator, and Track 2 V3 suites." Alt text: "Matrix listing invalid input, transitions, polling, cancellation, backend cancel, stale runs, retry, reset, unmount, timers, provider unavailable, review-only, not-found, dish candidates, coordinator, and Track 2 closure, all marked pass with their evidence type."
